/*
  Optional EmailJS client integration placeholder.
  To use EmailJS client-side delivery, sign up at https://www.emailjs.com/ and set your IDs here.
  This file is intentionally minimal so the app works even if EmailJS is not configured.
*/
// Example:
// window.emailjsConfig = {
//   serviceID: 'your_service_id',
//   templateID: 'your_template_id',
//   publicKey: 'your_public_key'
//};

// If you want client-side emails, set window.emailjsConfig values and uncomment init below.
// if (window.emailjsConfig) {
//   emailjs.init(window.emailjsConfig.publicKey);
//}
